#include<stdio.h>
/**
 * main - printing of decimal numbers
 * Return: Always 0 (sucess)
 */
int main(void)
{
	int num;

	for (num = 0; num < 10; num++)
	printf("%d", num);

	putchar('\n');
	return (0);
}
