#include<stdio.h>
/**
 * main - printing of decimal numbers
 * Return: Always 0 (sucess)
 */
int main(void)
{
	int num;

	num = 0;

	for (num = 0; num < 10; num++)
	printf(num);
	print('\n');
	return (0);
}
